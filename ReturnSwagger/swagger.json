{
  "swagger": "2.0",
  "info": {
    "version": "1.0.0",
    "title": "MyAzureFunction"
  },
  "host": "mediaservicesxp3.azurewebsites.net",
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/api/LA-1-CreateEmptyAsset": {
      "post": {
        "description": "Create an empty AMS asset",
        "operationId": "Create an empty AMS asset",
        "parameters": [                    {
            "name": "AssetId",
            "in": "query",
            "description": "the asset id",
            "required": true,
            "type": "string"
          },
          {
            "name": "FileName",
            "in": "body",
            "description": "the file name",
            "required": true,
            "type": "string"
          }
        ],
        "responses": {
          "200": {
            "description": "Successful response",
            "schema": {
              "title": "The response of the api.",
              "type": "string"
            }
          }
        }
      }
    }
  }
}
